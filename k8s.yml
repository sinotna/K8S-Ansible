---
- hosts: K8S-Server
  become: yes
  roles:
    - role: system
      tags: system
    - role: k8s
      tags: k8s
    - role: docker
      tags: docker
        #    - role: "{{ gui_type }}"
        #      tags: gui
        #      when: gui == "yes" 
...
